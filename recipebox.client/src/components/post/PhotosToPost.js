import React from 'react';
import PropTypes from 'prop-types';

const PhotosToPost = (props) => {
	return (
		<div>
			<h1>Add photos to post in this component</h1>
		</div>
	);
};

PhotosToPost.propTypes = {};

export default PhotosToPost;
